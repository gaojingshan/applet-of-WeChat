function myslice(arr, a, b) {
    return arr.slice(a, b);
}

function checkInArr(arr, item) {
    return arr.indexOf(item) != -1;
}
module.exports = {
    myslice: myslice,
    checkInArr: checkInArr
}