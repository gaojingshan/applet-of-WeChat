function theOneItemInArray(arr, e) {
    for (var i = 0; i < arr.length; i++) {
        if (arr[i].e == e) {
            return arr[i]
        }
    }
    return {}
}
// 时间戳转换成日期对象
function timesDate(timestamp) {
    var dateobj = getDate(timestamp);
    return dateobj.getFullYear() + '-' +
        dateobj.getMonth() + '-' +
        dateobj.getDate()
}
module.exports = {
    theOneItemInArray: theOneItemInArray,
    timesDate: timesDate
}